os:
  - linux
language: generic
dist: trusty
notifications:
  email:
    on_success: never
    on_failure: change
install:
  - source ./.travis/install.sh
script:
  - swift build
  - swift build -c release
  - swift test